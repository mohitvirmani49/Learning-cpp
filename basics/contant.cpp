#include <iostream>

using namespace std;

int main() {
    const int value = 50;  // Constant means the value of the variable cannot be changed in the whole program 
    cout << "Value is " << value << "\n";
    
}
